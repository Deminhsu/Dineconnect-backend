spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/dinner
    username: root
    password: a12345
#  security:
#    user:
#      name: admin
#      password: a1234

mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true

dinner:
  jwt:
    user-secret-key: ilovedinner
    user-ttl: 21600000 # 6 hours
    user-token-name: authentication
#    user-token-name: Authorization
file:
  upload-dir: "C:\\Users\\ken91\\Documents\\College\\Dineconnect-backend\\image\\"

# springdoc-openapi项目配置
#springdoc:
#  swagger-ui:
#    path: /swagger-ui.html
#    tags-sorter: alpha
#    operations-sorter: alpha
#  api-docs:
#    path: /v3/api-docs
#  group-configs:
#    - group: 'default'
#      paths-to-match: '/**'
#      # 注意包路径分割符是点，直接在IDEA中copy的路径是斜杠会导致配置不生效
#      packages-to-scan: com.xiaominfo.knife4j.demo.web
## knife4j的增强配置，不需要增强可以不配
#knife4j:
#  enable: true
#  setting:
#    language: zh_cn